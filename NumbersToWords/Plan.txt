
/*
 Thoughts

- Strip lead zeroes
- Recursive every thousand (3 digits)
    - pass next 3 digit number
    - return english description
    - outside of procedure track and append ("", thousand, million, billion) to desc
- Dictionary for 0 to 19, 20, 30, ..., 90, 100, 200, ..., 900
- Special for other numbers ending in 0 - omit the zero fm description

*/

NSString *numGroupName = ["", "Thousand", "Million", "Billion"];

int numGroupNameIndex = 0;

(NSString *) getWordsFromNumber:(int)num {
    while () {
    }
}
